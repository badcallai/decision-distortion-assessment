# Copy this file to .env.local and fill in real values.
# All server-only — no NEXT_PUBLIC_ prefix, so these never reach the browser.
# Also set the same variables in Vercel's project settings.

# Supabase (database)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Resend (transactional email — report delivery)
RESEND_API_KEY=
